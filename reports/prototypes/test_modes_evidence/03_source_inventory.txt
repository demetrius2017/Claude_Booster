OBSERVATION UTC: 2026-08-19T13:28:00Z; source inspection only.
OPERATION: read-only repository scans; allowlist decision=ALLOW (rg/find/sed/git config).

KEY RESULTS:
- phase.py defines six valid phases RECON, PLAN, IMPLEMENT, AUDIT, VERIFY, MERGE; no progress subcommand exists despite command templates invoking progress.
- get_phase reads <root>/.claude/.phase as a single uppercased token and returns RECON for missing, invalid, unreadable, or malformed state. set_phase uses direct write_text and appends a best-effort UTC line to phase_transitions.log. There are no fields for schema, expiry, root binding, issuer, session/run identity, lock, or atomic replacement.
- Direct phase consumers outside reports/docs: .gitignore, ARCHITECTURE.md, README.md, templates/codex/skills/booster-command/SKILL.md, templates/commands/{audit,code-review,consilium,go,hackathon,phase,start}.md, templates/scripts/{delegate_gate,fable_autopilot_state,go_gate,phase,phase_gate,phase_prompt_inject,preserve_plan_context,statusline.sh,test_delegate_gate_advisory.sh,test_delegate_gate_msg.sh,test_go_gate.sh,test_go_gate_v2.py}, tests/{test_delegate_gate_phase.sh,test_delegate_gate_ttl.sh,test_fable_usage.sh,test_go_gate.sh,test_lead_epistemic_anchors.py,test_statusline.sh}.
- Test roots: 37 Python test files and 47 shell test files. Python includes pytest-only test_slice_calibration.py and unittest suites under templates/scripts/supervisor/tests; shell suites are direct Bash acceptance/contract checks. Many tests use temporary repos/files; some explicitly access $HOME/.claude or SQLite, and the codex_wait_patch scripts use git clone. Test registry cannot assume pytest-only or that all commands are hermetic.
- .github is absent (find returned: No such file or directory). Therefore no in-repository workflow supplies protected CI enforcement evidence.
- install.py atomic_write uses mkstemp, write/flush/fsync, Darwin F_FULLFSYNC when available, chmod, os.replace. slice_ledger_core._atomic_projection adds parent-directory fsync; _append opens O_NOFOLLOW|O_APPEND and fsyncs. install.py manages Claude settings JSON hooks, not Git hooks: no pre-push/hooksPath/.git/hooks/core.hooksPath occurrence exists in install.py.
- slice_git_core reusable public primitives: canonical_path, parse_porcelain_v2, stable_git_state, file_fact, snapshot, facts_compatible, classify, attribution_receipt. It captures HEAD/tree/ref/index SHA-256/object format and double-samples state. It does not resolve a push destination, test registry, release manifest, candidate worktree commit, receipt storage, or protected CI trust.
- slice_ledger_core has secure no-follow reads, canonical JSON/hash-chain validation, atomic projection, and fsynced append. Its functions are private implementation primitives and it models slice lifecycle, not test receipts/leases.
