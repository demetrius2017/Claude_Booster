# Handover — 2026-07-07 13:21

## Goal + KPI

- **North Star:** Claude Booster makes Claude Code a compounding, low-friction daily driver — including honest, always-visible cost/limit telemetry so the operator never flies blind on spend or plan budget.
- **Current milestone:** Fable cost telemetry must reflect *real* money. After the 2026-07-07 cutover Fable moves from included-in-Max to metered usage-credits; the widget's month-to-date figure must show only *billable* credits, not the free-window shadow estimate. Plus: give Codex sessions the same rich status surface as Claude, without duplicating info.
- **KPI:** (1) `m$` on both surfaces (`fable_tmux_status.sh`, `statusline.sh`) reads `mtd_credits` (post-cutover), not the raw MTD total. Verified: `mtd_included=$914.30 / mtd_credits=$0.00` → widget shows `m$0`. (2) `tests/test_fable_status.sh` = 20/20 exit 0; deployed `~/.claude/scripts/*` byte-identical to `templates/scripts/*` (test A9). (3) Codex dark statusline enriched via native `[tui].status_line` (config parses, exit 0). **All met.**

## Summary

Session was a live status-surface debugging/UX thread driven by Dmitry watching the tmux bar + both client statuslines. Four things shipped:

1. **Fable `m$` = billable credits only.** Root question: is Fable already burning credits? Answer: no — the whole $914 MTD sat in the *included* window (≤ 2026-07-07); $888 of it was a single day (2 июля, 2164 events, when Fable was briefly the primary coder). Added `FABLE_CREDIT_CUTOVER_UTC = 2026-07-08T00:00:00Z` and split MTD into `mtd_included` (free) vs `mtd_credits` (real money) in `fable_usage.py`. All three renderers (`fable_tmux_status.sh`, `statusline.sh`, `fable_usage.py::brief_lines`) now show `mtd_credits`. Currently `m$0` — correct, nothing billable yet. Fable pricing after cutover: **$10/Mtok input, $50 output** (2× Opus), metered usage-credits.

2. **tmux `status-right-length` 40 → 80.** The bar truncated pane_title + clock after the Fable segment (`... d$6.20 | "horiz`). Bumped the cap; ALSO discovered (code-over-docs) that `status-right` was live-only, NOT in `~/.tmux.conf` despite the memory note — restored the full `set -g status-right '...'` block + length 80 + interval 15 into `~/.tmux.conf` so it survives a server restart.

3. **Codex dark statusline enriched (native).** Corrected an earlier wrong claim: Codex CLI 0.142.5 **does** support a custom statusline via `[tui].status_line` (array of built-in item IDs; `/statusline` is the interactive front-end). Set `["model-with-reasoning","current-dir","git-branch","context-used","five-hour-limit","weekly-limit"]` in `~/.codex/config.toml`. Codex-native limits/context/branch now live in Codex's own line — no green-bar duplication. NB: our Fable *estimate* cannot go there (built-in items only, not command-backed).

4. **Reverted the green tmux 2nd row.** Briefly added a `status 2` + `tmux_row2.sh` (git-branch/cwd/Fable-breakdown). Dmitry preferred enriching the dark client statuslines over expanding the green bar (and it duplicated info). Fully reverted: `status on`, conf block removed, `tmux_row2.sh` deleted (deployed + template).

## Tools used

- `Bash` — sqlite3 split queries, tmux `set/show-options/source-file/refresh-client`, `codex features list` (config validation), `python3 -c tomllib`, test suite run.
- `Read`/`Edit`/`Write` — `fable_usage.py`, `fable_tmux_status.sh`, `statusline.sh`, `~/.codex/config.toml`, memory note, this handover.
- `WebSearch`/`WebFetch` — Fable pricing/cutover (Anthropic + press), Codex `[tui].status_line` semantics (OpenAI docs + GitHub issues).
- `AskUserQuestion` — chose the (later reverted) green-2nd-row approach.
- No agents/workers spawned (Lead-direct: config + tooling edits, all < ~40 lines per file, fully-read files).

## First step tomorrow

```bash
cd /Users/dmitrijnazarov/Projects/Claude_Booster && python3 ~/.claude/scripts/fable_usage.py summary --json 2>/dev/null | python3 -c "import sys,json;d=json.load(sys.stdin);print('credits=\$'+d['mtd_credits']['cost_usd'],'included=\$'+d['mtd_included']['cost_usd'])"
```
This confirms whether real Fable credits have started accruing (credits > $0 means we are past cutover and burning metered credits — the `m$` widget will light up). If credits jump unexpectedly, check `~/.codex/config.toml` model routing and the horizon project's Fable usage.

## Problems / Solutions

- **P: Widget `m$914` read as real spend.** S: split MTD by cutover; `m$` now = `mtd_credits` ($0). The $914 is the *included* shadow estimate, never billed.
- **P: tmux bar truncated title/clock.** S: `status-right-length` 40→80; verified rendered width 75 ≤ 80.
- **P: memory note said `status-right` lives in `~/.tmux.conf` — it did not (live-only).** S: code-over-docs; restored it into the conf and updated the memory note.
- **P: I twice wrongly claimed Codex has no custom statusline.** S: verified against the native binary + OpenAI docs — it has `[tui].status_line`; configured it. Proactively corrected in-thread.
- **P: `/statusline` menu showed only 2 of 6 items checked.** S: that Codex session predated the config edit; the on-disk `config.toml` has all 6. A fresh Codex session (or re-selecting in `/statusline` and saving) applies them. ⚠️ Saving the menu as-is would overwrite the 6-item config down to what's checked.

## Outstanding Debts

| # | Status | Item | Owner / next action |
|---|--------|------|---------------------|
| 1 | BLOCKED-EXTERNAL | Codex enriched `[tui].status_line` needs a **fresh Codex session** (or `/statusline` re-save) to render — config-on-disk is correct but the running session predates it. | Dmitry: restart Codex or Esc the menu then relaunch. Not code work. |
| 2 | OPEN (user decision) | Green tmux `row0` still shows `5h/7d` limits, now duplicated by both dark statuslines. Proposed: strip limits → `F l$ d$ | title clock` (Fable-only, unique). | Awaiting Dmitry's yes/no. Low priority. |

## Required reading

- `reports/handover_2026-07-07_132100.md` (this file) — full session context; the `m$`-credits split and the Codex `[tui].status_line` discovery are the two load-bearing changes.
- `~/.claude/scripts/fable_usage.py` — new `FABLE_CREDIT_CUTOVER_UTC` constant + `mtd_included`/`mtd_credits` split in `build_summary()`. Any status work MUST read `mtd_credits` for billable money, not `mtd`.
- `~/.claude/projects/-Users-dmitrijnazarov-Projects-Claude-Booster/memory/project_dmitry_status_surface_is_tmux.md` — updated: `status-right-length` now 80 (was 40), `status-right` restored into `~/.tmux.conf`, pane_title still capped at 21.
- `~/.codex/config.toml` — new `[tui].status_line` block; do not let a `/statusline` save silently trim it. Item IDs are Codex-native built-ins only.

## Session reference

Session UUID: ce66b459-756b-44bd-8118-9eaddc27413b
JSONL: /Users/dmitrijnazarov/.claude/projects/-Users-dmitrijnazarov-Projects-Claude-Booster/ce66b459-756b-44bd-8118-9eaddc27413b.jsonl

Grep this JSONL during RECON to see exactly what was tried (incl. the reverted green-2nd-row experiment and the two wrong "Codex has no statusline" claims + their corrections) and the verify commands run.
