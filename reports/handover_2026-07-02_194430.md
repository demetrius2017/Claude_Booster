# Handover 2026-07-02 19:44:30 - Fable consult command

## Goal + KPI

- **North Star:** Claude Booster should let Dmitry route judgment to the strongest model when needed without turning that model into the default budget lane.
- **Current milestone:** Make "посоветуйся с Fable 5" unambiguous for every Lead: one read-only Fable consult, not a consilium, not a routing change, and not automatic file edits.
- **KPI:** `$fable`, `/prompts:fable`, `/fable <question>`, and natural-language "посоветуйся с Fable 5" resolve to a single read-only advisory command; install bridge tests pass with the new alias counts; live `~/.claude`, `~/.agents`, and `~/.codex` copies are synced.

## Summary

This mini-session followed the previous model-routing burn-rate handover and added the missing protocol layer for Fable.

The practical problem was linguistic, not architectural: when Dmitry writes "посоветуйся с Fable 5", agents were treating it as if he asked for a full consilium or as if Fable should become the default route again. That is wrong. The new contract is now explicit:

- "посоветуйся с Fable 5", "спроси Fable", `Fable opinion`, `/fable`, and `$fable` mean exactly one read-only Fable 5 advisory consult.
- It is **not** `/consilium`: no 3-5 agents, no `reports/consilium_*.md`, no model-balancer changes.
- Fable gives advice; the Lead remains responsible for synthesis and the next action.
- If Fable is unavailable, the Lead says so plainly and does not silently substitute Codex, PAL, Z.ai, Grok, Opus, or Sonnet.

Repository changes shipped:

1. Added `templates/commands/fable.md` with the one-off Fable consult procedure and prompt template.
2. Added Codex aliases:
   - `templates/codex/skills/fable/SKILL.md`
   - `templates/codex/prompts/fable.md`
3. Updated `templates/codex/skills/booster-command/SKILL.md` so the command runner advertises `fable`.
4. Updated `templates/rules/tool-strategy.md` with the always-loaded Lead rule for natural-language Fable consults.
5. Updated `AGENTS.md` so repo-local Codex instructions recognize `fable` as a Booster command.
6. Updated `tests/test_install_codex_bridge_integration.py` expected counts from `17/16/16` to `18/17/17`.
7. Synced live installed copies:
   - `~/.claude/commands/fable.md`
   - `~/.claude/rules/tool-strategy.md`
   - `~/.agents/skills/fable/SKILL.md`
   - `~/.agents/skills/booster-command/SKILL.md`
   - `~/.agents/skills/booster-command/references/commands/fable.md`
   - `~/.codex/prompts/fable.md`
8. Saved rolling-memory directive `46055` with the same rule, so future sessions can recover it even outside this checkout.

Commit pushed:

```text
36658fe feat: add fable consult command
```

No `roadmap.html` or `roadmap.md` exists in this repo. Roadmap update was skipped rather than creating a new roadmap file solely for the handover ritual.

## Tools used

- `sed`, `rg`, `find`, `stat`, `ls`, `date`
- `git`
- `python3 tests/test_install_codex_bridge_integration.py`
- rolling memory MCP `memory_search` and `memory_memorize`
- `apply_patch`

## Verification

Latest verification before this handover:

```text
python3 tests/test_install_codex_bridge_integration.py
exit=0
Results: 10 passed, 0 failed

rg validation of repo and live installed copies
exit=0
Found fable command/skill/prompt references, "not consilium", "one-off read-only",
and command `fable` in templates plus ~/.claude, ~/.agents, ~/.codex.

git ls-remote origin refs/heads/main
exit=0
36658feafd5a2516c8675705a6a5e11175de5dc8 refs/heads/main

git status --short
exit=0
Clean before writing this handover.
```

## Problems / Solutions

| Problem | Solution |
|---|---|
| Agents interpreted "посоветуйся с Fable 5" as a possible consilium trigger or default-model escalation. | Added a dedicated `/fable` command and always-loaded tool-strategy rule: one read-only Fable consult, not consilium. |
| The previous Fable burn guard made Fable permissioned, but did not define what counts as explicit permission for a one-off consult. | Natural-language Fable phrases now count as explicit permission for exactly one consult on the current question. |
| Codex bridge would not install the new alias unless templates and tests knew about the extra command. | Added skill/prompt aliases and updated install integration expected counts to `18` skills, `17` prompts, `17` command specs. |
| Live sessions would still miss the new behavior if only repo templates changed. | Synced live files under `~/.claude`, `~/.agents`, and `~/.codex`, and stored memory directive `46055`. |

## Outstanding Debts

No outstanding debts - session clean.

## Required reading

- `reports/handover_2026-07-02_194430.md` - this handover; start here for the Fable consult protocol.
- `templates/commands/fable.md` - source of truth for the one-off Fable consult command.
- `templates/rules/tool-strategy.md` - always-loaded Lead behavior, including natural-language "посоветуйся с Fable 5".
- `templates/codex/skills/fable/SKILL.md` - Codex skill alias for `$fable`.
- `templates/codex/prompts/fable.md` - Codex prompt alias for `/prompts:fable`.
- `templates/codex/skills/booster-command/SKILL.md` - command runner discovery list; must include `fable`.
- `tests/test_install_codex_bridge_integration.py` - install bridge regression test; expected counts now include the Fable alias.
- `reports/handover_2026-07-02_180246.md` - prior model-routing handover; explains why Fable remains permissioned and Codex gpt-5.5 is the default lane.

## Session reference

Session UUID: `019f2368-d2fb-7303-b31f-753b7ea63a0d`  
JSONL: `/Users/dmitrijnazarov/.codex/sessions/2026/07/02/rollout-2026-07-02T18-18-20-019f2368-d2fb-7303-b31f-753b7ea63a0d.jsonl`

This JSONL can be grepped during RECON to understand the exact sequence of checks, edits, verification, commit, and push in this session.

## Recent git log

```text
36658fe feat: add fable consult command
f84b89b chore: handover model routing burn guard
ed4857d feat: add provider health routing fallback
```

## First step tomorrow

Run:

```bash
cd /Users/dmitrijnazarov/Projects/Claude_Booster && \
sed -n '1,140p' templates/commands/fable.md && \
rg -n 'посоветуйся с Fable 5|command `fable`|not consilium|one-off read-only' \
  AGENTS.md templates/commands/fable.md templates/rules/tool-strategy.md \
  templates/codex/skills/fable/SKILL.md templates/codex/prompts/fable.md && \
python3 tests/test_install_codex_bridge_integration.py
```

Expected:

- `/fable` contract says read-only advisory consult, not consilium.
- Tool-strategy natural-language rule is present.
- Install bridge test exits `0` with `10 passed, 0 failed`.
