# Handover — 2026-06-14 (Claude_Booster) — Шестёрка dogfooded on real debts

> Continuation of the same session as `handover_2026-06-13_200806.md` (which shipped the SHIP-1..4 pipeline + KPI + RECON-as-review). This handover covers the work AFTER that: running the new Шестёрка `/go` on two real debts end-to-end, plus a false-alarm closure.

## Summary

The new six-stage `/go` was dogfooded on **two real debts back-to-back, both first-pass-clean**, and in **both** runs the cross-provider Phase-1B challenge (Opus) caught a genuine defect/contradiction *before any code* — the SHIP-1 thesis proven twice more on live tasks.

1. **Debt #2 — provider-aware Flow Designer spawn (commit `c74ea1d`).** go.md Phase 1 only said "Spawn ONE Flow Designer agent" — undefined when `get hard`=codex-cli (Agent tool spawns Claude only). Added a "Spawn mechanics by provider" table mirroring Phase 2 (anthropic→Agent; codex-cli→`codex_worker.sh`, the TEXT channel, NOT sandbox). **Challenge caught:** the Verifier's grep would have falsely passed on the *unedited* file (ambient `provider`/`hard` tokens) → forced a git-baseline **differential** assertion; also the `codex_worker.sh`⊂`codex_sandbox_worker.sh` substring trap. 14/14, diff-review no HIGH.

2. **Debt #1 keystone — Codex telemetry capture (commits `8b6263d` + `c1d89f4`).** `model_metric_capture.py` logged every Codex Bash call with duration=NULL, category="medium" (hardcoded) → balancer had zero signal → coding/hard pinned. This was a BROKEN CAPTURE, not "waiting for data" (RECON corrected the premise). Now: real `duration_ms` (0→0, bool-reject, absent→NULL), `num_turns=1`, category from a leading `CLAUDE_BOOSTER_TASK_CATEGORY=<cat>` prefix, `CLAUDE_BOOSTER_METRICS_DB` test override. go.md wires the prefix on all 4 Codex calls (FD/Challenge/Review→hard, Worker→coding). **Challenge caught (best yet):** a `VAR=val ` prefix would break `_match_codex_command`'s `(?:^|[/;&|])` anchor → matcher None → NO ROW (the feature self-defeats), and my AC's "parse prefix" vs "don't touch matcher" was a contradiction. Resolved pre-code via `_strip_leading_env` → match on the bare command (matcher byte-unchanged; also fixed a pre-existing `FOO=bar codex_worker.sh` non-match). 11/11, diff-review CLEAN.

3. **Debt [1] (kpi pollution) — FALSE ALARM, closed (commit `63efbb3`).** My `/debt list` flagged "2 polluting rows" in `kpi_rework.jsonl`. RECON before the would-be `/go` disproved it: the rows are **legitimate cross-project `/go` outcomes** (CRM_AI + AINEWS — the log is global by design); the test does NOT leak (uses `CLAUDE_BOOSTER_LOGS_DIR`). No data deleted. The real fix was the root of the confusion: `report` now **defaults to the current project** (Booster: 3 tasks, 100% first-pass-clean), `--all` for the global view. 69/69.

## Goal + KPI

- **North Star:** Claude Booster — make Claude Code (and Codex) remember, learn, and audit itself across sessions, with one canonical install.
- **Current milestone:** A measurable, cross-provider quality pipeline — proven on real work. The Шестёрка now has a track record (2 real dogfoods, both first-pass-clean, both with a design-time save).
- **KPI (instrumented, evidence):** `kpi_rework.py report` (now project-default) shows **Booster: 3 tasks, first_pass_clean_rate=100%, 0 defect-categories** — the design-time gates are doing their job (the challenge caught the would-be rework before it became a Worker retry, so it never counts as a `verifier-fail`). `--all` view: 5 tasks / 80% (includes other projects). Extra model spend $0 (Codex flat-fee; Opus only as Lead + cross-provider checkers).

## Tools used

- `/go` ×2 (dogfood) — FD (Codex via `codex_worker.sh`), Challenge/Verifier/Diff-review (Opus Agent), Worker (`codex_sandbox_worker.sh` → diff applied via `git apply`).
- `Edit`/`Write` — go.md (Phase 1 table + 4 category prefixes), model_metric_capture.py + its test, kpi_rework.py + its test, memory files.
- `install.py --codex-bridge --yes` — re-synced the bridge after each go.md edit.
- `kpi_rework.py record/report`, `phase.py`, `git` (4 commits, 2 pushes).

## Problems / Solutions

- **Debt #1 was mis-framed as data-gated.** RECON showed the capture was broken (all-NULL Codex rows). Fixed the capture; the *unpin* is what's now time-gated.
- **AC contradiction (prefix vs unchanged matcher)** — caught by the challenge, resolved by Lead pre-code (strip-then-match). Did NOT bounce "A or B?" to the user (51% rule).
- **Changing `report`'s default scope broke 3 test cases** (synthetic `project="unknown"` rows reported from Booster's cwd). Resolved by adding `--all` to the two aggregation cases (they test the engine, not scoping) — NOT by reverting the intended default. 69/69.
- **`git commit -m` with backticks/`$` corrupted a message earlier this session** — all commits since use `git commit -F <file>`.

## Outstanding Debts

| # | Priority | Description | Status |
|---|----------|-------------|--------|
| 1 | LOW | RECON-as-review harvest is circular on "fix X" tasks — it surfaces the Objective itself as an `adjacent_finding` (seen on debt#2 and #1). go.md/PFD: the harvest should skip findings that restate the Objective | open |
| 2 | LOW | go.md (from debt#2) diff-review advisories: Phase 1 parameterizes `<model>` while 1B/3B pin `gpt-5.5`; "piping"/`<` phrasing slip. Cosmetic | open |
| 3 | MED | Debt #1 FINAL — unpin coding/hard. Capture is fixed; now purely **time-gated** (~14d of real coding/hard Codex metrics), then remove them from `_PINNED_CATEGORIES`. NOT agent-actionable now | deferred (time-gated) |

### Blocked — needs human (not agent-actionable)

| # | Needs from you |
|---|----------------|
| B1 | `weekly_tokens_cap` — your Claude Max weekly token allowance (a number only you know). Until set, the balancer uses the stale 86% snapshot (harmless). |

## Required reading

- `reports/handover_2026-06-14_142128.md` — this file.
- `reports/handover_2026-06-13_200806.md` — the prior handover (same session; the SHIP-1..4 pipeline build). Read its Summary for the pipeline shape.
- `templates/commands/go.md` — Phase 1 now has the provider-aware spawn table; all 4 Codex invocations carry `CLAUDE_BOOSTER_TASK_CATEGORY=<cat>` (debt #1 wiring). The pipeline is the Шестёрка (6 stages).
- `templates/scripts/model_metric_capture.py` — Codex telemetry now real; `_strip_leading_env` → match on bare command (matcher unchanged); `_get_db_path()` honors `CLAUDE_BOOSTER_METRICS_DB`. Test: `test_model_metric_capture.py` (11 cases).
- `templates/scripts/kpi_rework.py` — `report` defaults to current project; `--all` = global. Test: `test_kpi_rework.py` (69).
- `memory/` — `project_debt1_codex_telemetry.md` (unpin is now time-gated only), `project_ship1_challenge_gate.md`, `feedback_recon_as_review_scoped_debt.md`.

## Session reference

Session UUID: `62c1ae5f-70bc-4929-8cdb-ca712cc8a038`
JSONL: `/Users/dmitrijnazarov/.claude/projects/-Users-dmitrijnazarov-Projects-Claude-Booster/62c1ae5f-70bc-4929-8cdb-ca712cc8a038.jsonl`

Grep this JSONL during RECON to see the two full Шестёрка dogfoods (FD/challenge/worker/verifier/review spawns, the contract-contradiction catch on debt #1, the strip-then-match resolution) and the false-alarm RECON on the kpi "pollution".

## First step tomorrow

Confirm both dogfooded artifacts are intact and the KPI loop reports per-project:

```bash
cd ~/Projects/Claude_Booster
python3 templates/scripts/test_model_metric_capture.py | tail -1   # expect: Results: 11 passed, 0 failed
python3 templates/scripts/test_kpi_rework.py | tail -1             # expect: Results: 69 passed, 0 failed
python3 ~/.claude/scripts/kpi_rework.py report                     # expect Booster-scoped (~3 tasks, 100%), NOT global
grep -c 'CLAUDE_BOOSTER_TASK_CATEGORY=' templates/commands/go.md   # expect 4 (debt #1 wiring)
```

Then either: pick up **debt LOW #1** (teach the RECON-as-review harvest to skip Objective-restating findings — a real pipeline-quality improvement, cheap), or wait out **debt #1 FINAL** (the coding/hard unpin — check `model_balancer.py` once ~14d of real Codex coding/hard metrics have accumulated; nothing to do until then).
