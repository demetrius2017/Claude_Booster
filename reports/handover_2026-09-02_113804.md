# Handover — External review channels restored (Grok probe, routed-worker refusal, GLM plan)

## Summary

Dmitry reported that Codex lost all independent external reviewers: PAL/GPT and GLM-5.2 returned 429, Grok was "not configured". Direct probes showed three separate causes. PAL/OpenAI and Z.ai were out of credits (billing, not code). Grok worked on the host but Codex could not use it because the specs named no availability probe (Codex invented `grok_cli.py status`, which did not exist) and `codex_routed_worker.py` silently launched Codex for grok-cli routes, so "independent" tertiary audits were same-provider output. Shipped and installed commit `ee8a1a3`: `grok_cli.py status`/`auth-status` probe, typed failure events shared via `provider_failure_log.py`, `max_turns` classification, credential-shaped redaction, routed-worker refusal with exit 65, spec/skill wording, test isolation (`tests/conftest.py`), registry entry. Two-round external audit via Grok (budget 24) plus three-lens Codex Terra code review. Dmitry bought Z.ai Lite yearly; GLM smoke returns `OK` and the balancer cleared all degraded providers. PAL/OpenAI still has no credits.

## Goal + KPI

- **North Star:** every Booster audit/consilium verdict is backed by at least one genuinely independent external model, never by the same provider relabelled.
- **Current milestone:** restore and harden the external reviewer chain PAL → Z.ai → Grok in Codex and Claude, with availability probes and failure telemetry the balancer can act on.
- **KPI:** 0 audits where a non-Codex route is executed by Codex (routed worker exit 65 on grok-cli/zai-cli); Grok `audit_tertiary` success rate ≥ 80% over 7 days (was 1/6 on 2026-09-01); `failure_type` populated for 100% of Grok failures.

## Tools used

- Direct probes: OpenAI chat completions (HTTP 429), `zai_cli.py smoke`, `grok_cli.py smoke|review|status`, `codex_routed_worker.py`, sqlite `model_metrics`, Codex session JSONLs under `~/.codex/sessions/2026/09/02`.
- Agents: worker-code, worker-docs, worker-apply, worker-fix2, worker-isolation, worker-registry, worker-maxturns, worker-fix3 (all Opus); verifier-docs, verifier-code, verifier-final (Opus, read-only receipts).
- Code review: three lenses via `codex_worker.sh gpt-5.6-terra`. External audit: Grok `grok_cli.py review --budget-turns 24` (two rounds). PAL and Z.ai unavailable during the session.
- `install.py --yes` twice; backup `~/claude_backup_2026-09-02_pre_grok_fix.tar.gz`.

## Verification evidence

- `python3 -m pytest tests -q` → 318 passed, 3 errors (pre-existing `tests/test_lead_epistemic_anchors.py`, missing `tmp` fixture), exit=1 due to those errors only.
- `bash tests/test_audit_zai_smoke.sh` → PASS=14 FAIL=0; `bash tests/test_model_balancer_all.sh` → 7/7 suites, 127 assertions, exit=0; `bash tests/test_model_tag_enforcer.sh` → 26 passed.
- `~/.claude/scripts/grok_cli.py status` → `status=available`, exit=0; `GROK_BIN=/nonexistent/g … status` → exit=127 with `failure_type=binary_missing` event.
- `printf x | ~/.claude/scripts/codex_routed_worker.py audit_tertiary …` → exit=65, no Codex banner.
- Grok second-round audit with `--budget-turns 8` → exit=1 `Max turns reached`; with `--budget-turns 24` → exit=0, `VERDICT: FAIL` findings fixed, final probes PASS.
- Z.ai after payment: `zai_cli.py smoke` → `OK`; `model_balancer.py get audit_secondary` → `zai-cli:glm-5.2`; `provider_health` → `{}`.
- Real `~/.claude/logs/model_provider_failures.jsonl` line count unchanged across suites (163 → 163).

## Problems / Solutions

- **Grok "not configured" in Codex** → probe command did not exist; added `status`/`auth-status`, specs reference it explicitly and forbid inferring from env vars.
- **Routed worker substituted Codex for Grok** → `_route()` required `reasoning_effort` for every provider; now only for codex-cli, non-Codex routes exit 65 naming the correct runner.
- **Grok failures undiagnosable** → typed events to the shared failure log; `Max turns reached` classified as `max_turns`; ANSI stripped; specs say `--budget-turns 24` for repo-reading audits.
- **First Grok audit found a deleted shebang, stale `test_audit_zai_smoke.sh`, stale `model_tag_enforcer.py` wording** → all fixed; smoke checks grew 12 → 14.
- **Second Grok audit found NameError in the adapter test runner, redaction over-matching (`api_response_code` → `[redacted]`), C14 writing to shared `/tmp`** → fixed; redaction now requires credential-shaped values.
- **A grok test polluted the real failure log** (permanent `binary_missing` event) and flipped a balancer test → Dmitry authorised removal (backup `model_provider_failures.jsonl.bak.2026-09-02`); `tests/conftest.py` now redirects log and metrics DB for every test.
- **GitHub push protection rejected a Slack-token-shaped test fixture** → fixture rewritten as a constructed string; commit amended before push.
- **Registry contract test failed** because the new test file was unregistered → added to `py-contracts-all.source_files`.

## Outstanding Debts

| # | Priority | Description | Status |
|---|----------|-------------|--------|
| 1 | MED | `tests/test_lead_epistemic_anchors.py` has 3 errors from a missing `tmp` fixture (pre-existing); fix or register as known-failing. | open |
| 2 | MED | Untracked `patches/`, `scripts/codex_wait_patch/`, `docs/codex_wait_patch.md` are pinned to Codex 0.145.0-alpha.13 while installed Codex is 0.152.1; decide rebase vs drop. | open |
| 3 | LOW | Shadow-mode timing calibration for DEVELOPMENT test mode (carried from 2026-08-19). | follow-up |
| 4 | LOW | Grok `status` exit 69 (auth missing) and review exit 69 (empty response) share a code; documented, consider splitting. | open |

### Blocked — needs human

| # | Needs from you |
|---|----------------|
| B1 | Top up OpenAI API credits so PAL/GPT-5.5 returns (currently `429 credit_balance_exhausted`). |

## Required reading

- `reports/handover_2026-09-02_113804.md` — this file; session outcome and next step.
- `templates/scripts/grok_cli.py` — new `status` contract, failure classification, redaction rules; exit codes are load-bearing for specs.
- `templates/scripts/codex_routed_worker.py` — non-Codex routes now refuse with exit 65; do not "fix" that back into a fallback.
- `templates/scripts/provider_failure_log.py` — shared failure-event writer consumed by `model_balancer.py`; `CLAUDE_BOOSTER_PROVIDER_FAILURES_LOG` override is what keeps tests off the real log.
- `tests/conftest.py` — autouse isolation of failure log and metrics DB; every new test inherits it.
- `templates/commands/audit.md` — runner rule and Grok probe wording that Codex now follows.

## Slice telemetry

- Capability: Claude hooks/wrappers advisory; native Codex observational/no enforcement.
- Ledger run/session: `ef508f15-e8c3-4318-8c5f-043d4e3140c2` / `019f7674-3f34-7a40-b9c9-b52ad26a2020`; state `closed`, terminal disposition `blocked` (historical slice, unrelated to this session).
- Receipt path: `.claude/state/runs/f67cac37ef55ea242313c3e3751f1f44821b3a50e89129c82240a6838a94c63b/slice_telemetry.json`; receipt/hash absent (`status=not_recorded`), provider/adapter not recorded, coverage `unknown`.
- Record command (adapter `claude`, this session's transcript):

```bash
python3 '/Users/dmitrijnazarov/.claude/scripts/slice_telemetry.py' --cwd '/Users/dmitrijnazarov/Projects/Claude_Booster' record --provider 'claude' --transcript '/Users/dmitrijnazarov/.claude/projects/-Users-dmitrijnazarov-Projects-Claude-Booster/c33c37e4-dd21-48d2-9af6-cb519b533011.jsonl' --run-id 'ef508f15-e8c3-4318-8c5f-043d4e3140c2' --session-id '019f7674-3f34-7a40-b9c9-b52ad26a2020'
```

## Session reference

Session UUID: `c33c37e4-dd21-48d2-9af6-cb519b533011`  JSONL: `/Users/dmitrijnazarov/.claude/projects/-Users-dmitrijnazarov-Projects-Claude-Booster/c33c37e4-dd21-48d2-9af6-cb519b533011.jsonl`

This JSONL can be grepped during RECON to see the direct probes, the two Grok audit rounds and what each worker changed.

## First step

Confirm the chain end to end from Codex (restart Codex first so the reinstalled bridge loads), then check that Grok audits stop failing on turn budget:

```bash
~/.claude/scripts/grok_cli.py status; echo exit=$?; printf 'Reply with exactly: OK\n' | ~/.claude/scripts/zai_cli.py smoke; sqlite3 ~/.claude/rolling_memory.db "SELECT date(ts_utc), provider, task_category, success FROM model_metrics WHERE provider IN ('grok-cli','zai-cli') AND ts_utc > '2026-09-02' ORDER BY ts_utc DESC LIMIT 10"
```
