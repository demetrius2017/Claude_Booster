# Handover — developer/lead modes and private/public publication

## Summary

Codex and Claude Code now have explicit developer/lead session modes. Developer
implements and integrates directly, delegating useful research and independent
testing. Lead reuses devops/frontend/backend/tester sessions within one
conversation. The main agent owns the complete user-visible result in both modes.
Source implementation: `e758b5f`, `391e5ff`; publication documentation: `490a5e8`.

On 2026-09-16 Dmitry confirmed: «в реальности так работать лучше». Preserve this
working preference. It is qualitative feedback, not a measured speed/token claim.
Feedback is saved in rolling_memory, row 62606.

Public distribution commit: `eef61f7` on public/main. It contains 46 changed
distribution files, including the previously unpublished bounded loop, Astra
routing, business-first hints, and role modes. Published from a separate worktree
based on public/main; no new private reports or private commit ancestry included.
The public repository already contains historical reports; this operation did
not rewrite that history. Private main includes this handover and roadmap update.

Local experiment remains installed: Codex AGENTS.override.md masks the older
global instructions; Booster skills are explicit-only. Claude excludes the global
rules directory and disables built-in auto-memory; rolling_memory MCP remains
available. Claude hooks are retained. The small role-default snippets are installed
for both clients. Do not run the full installer on the real HOME as an incidental
update: it still installs the full rules, and role installation alone does not
enable commands-only/default snippets. Current turn changed no local settings.

## Goal + KPI

- North Star: deliver complete verified user outcomes with persistent knowledge
  and proportionate orchestration.
- Current milestone: publish selectable execution modes for both clients and
  preserve the successful local commands-only experiment.
- KPI: both distribution branches updated; installed bridge tests 11/11; relevant
  loop/routing tests 41/41; hook-anchor runner zero failures; memory and Claude
  hooks preserved. Time/token improvement remains unmeasured.

## Verification

- `PYTHONPATH=. python3 tests/test_install_codex_bridge_integration.py`: 11 passed,
  zero failed, exit=0. Temporary HOME only.
- `python3 -m pytest -q tests/test_codex_loop.py tests/test_codex_adapter_exact_route_contract.py tests/test_codex_capability_routing.py tests/test_gpt56_routing.py`:
  41 passed, exit=0.
- `python3 tests/test_lead_epistemic_anchors.py`: zero failures, exit=0.
- Initial combined pytest call incorrectly included the standalone anchor runner:
  43 passed, three fixture setup errors. Correct runner above passed; no test edits.
- `git diff --check`: private/public clean. Copied distribution files matched
  source bytes exactly before commit; reports excluded.
- Reused independent tester `/root/role_tester`: role contract PASS; no detected
  added credentials/private absolute paths in reviewed distribution diff. Two
  README findings fixed: default snippet activation and Astra consilium route.
- These checks do not claim an automated end-to-end fresh-model session test.

## Tools used

Git/exec, apply_patch, existing integration and regression runners, rolling_memory
search/memorize, persistent role_tester review. No new runtime dependencies.

## Problems / Solutions

- Blanket delegation transferred responsibility for patches without preserving
  ownership of the whole product. Developer mode keeps implementation/integration
  with the main agent; Lead explicitly retains whole-result accountability.
- Context loss from disposable workers: Lead reuses role IDs, lazily starts
  useful roles, and handles host capacity limits without claiming unlimited or
  cross-conversation persistence.
- Claude phase gate still requires IMPLEMENT for code edits. Role selection does
  not bypass it; normal phase transitions remain applicable.
- Public was seven commits behind. Exported the distribution delta on its existing
  branch rather than fast-forwarding private reports/history.

## Outstanding Debts

| Item | Status / next action |
|---|---|
| Quantitative developer vs lead effectiveness | Optional follow-up on real tasks; user preference is already clear. No benchmark required to keep current mode. |
| Commands-only installer preset | Not implemented; current setup is a reversible local experiment with manual documented snippets. |
| Existing wait-history Codex patch | Outside this task; roadmap still requires rebase before reuse. Untracked `docs/codex_wait_patch.md`, `patches/`, `scripts/codex_wait_patch/` preserved. |
| Historical slice telemetry | Closed July slice has no cached receipt; not evidence about this role-mode session. |

## Required reading

- `reports/handover_2026-09-16_121859.md`: current decision and publication evidence.
- `templates/commands/role.md`: mode ownership, persistent session reuse, switching.
- `templates/codex/skills/role/references/session-defaults.md` and
  `claude-session-defaults.md`: explicit activation; preserve hooks and permissions.
- `README.md`: installer/default distinction and both client invocation forms.

## Session reference

Session UUID: `01a0a160-5994-7053-9fe6-7812a872657e`
JSONL: `/Users/dmitrijnazarov/.codex/sessions/2026/09/14/rollout-2026-09-14T21-24-01-01a0a160-5994-7053-9fe6-7812a872657e.jsonl`.
This JSONL can be searched during RECON for earlier attempts and failures.
Recent eight-hour git log was empty at the start of publication; relevant earlier
implementation commits are named above.

## Slice telemetry

Capability: **Claude hooks/wrappers advisory; native Codex observational/no enforcement**.
Cached ledger belongs to an unrelated July slice: run
`ef508f15-e8c3-4318-8c5f-043d4e3140c2`, session
`019f7674-3f34-7a40-b9c9-b52ad26a2020`, state `closed`, terminal disposition
`blocked`. Status returned `not_recorded`, coverage `unknown`, receipt/hash null.
Expected receipt path:
`.claude/state/runs/f67cac37ef55ea242313c3e3751f1f44821b3a50e89129c82240a6838a94c63b/slice_telemetry.json`.
Provider/adapter for the existing historical JSONL: `codex_rollout_v1`.
Unknown event count is unavailable because there is no parsed receipt; do not
report zero. No transcript scan, backfill, closure, or autopilot change performed.

Concrete historical recording command, provided but not executed:

```sh
python3 '/Users/dmitrijnazarov/.claude/scripts/slice_telemetry.py' --cwd '/Users/dmitrijnazarov/Projects/Claude_Booster' record --provider codex_rollout_v1 --transcript '/Users/dmitrijnazarov/.codex/sessions/2026/07/18/rollout-2026-07-18T21-19-17-019f7674-3f34-7a40-b9c9-b52ad26a2020.jsonl' --run-id 'ef508f15-e8c3-4318-8c5f-043d4e3140c2' --session-id '019f7674-3f34-7a40-b9c9-b52ad26a2020'
```

## First step tomorrow

```sh
cd /Users/dmitrijnazarov/Projects/Claude_Booster
git status --short
git log -3 --oneline
```

Then use `$role status` in Codex or `/role status` in Claude. Continue real work
in developer mode; switch to lead explicitly when the task benefits from domain
agents. Do not recreate old sessions by IDs from another conversation.
