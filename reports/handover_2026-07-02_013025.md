# Handover — 2026-07-02 01:30 MSK / 2026-07-01 22:30 UTC

## Goal + KPI

- **North Star:** Claude Booster keeps Claude Code and Codex operationally aligned through memory, routing, verification, and handovers.
- **Current milestone:** close the Fable/iTerm routing update cleanly after the prior handover commit.
- **KPI:** repository is clean and pushed; runtime routing still launches the Lead, coding, and hard tiers on Fable; iTerm project tabs still launch Claude/Fable instead of Codex. Met.

## Summary

This is a follow-up handover after `reports/handover_2026-07-02_012207.md` was written, committed, and pushed.

The prior handover commit is on `main`:

```text
b88826f docs: add handover for Fable routing update
```

No additional source or runtime changes were made after that commit. I re-verified the state Dmitry asked for:

```text
model_balancer lead   -> anthropic:fable
model_balancer coding -> anthropic:fable
model_balancer hard   -> anthropic:fable
high_blast_radius     -> anthropic:claude-sonnet-4-6
iTerm DynamicProfiles -> remaining_codex_initial_text=[]; claude_model_fable_initial_text_count=12
git status            -> ## main...origin/main
```

No `roadmap.html` or `roadmap.md` exists, so roadmap update was skipped.

## Tools Used

- `git log --oneline --since='8 hours ago'` — confirmed the fresh handover commit.
- `git status --short --branch` — confirmed clean repo synced with origin.
- `model_balancer.py get` — confirmed Fable routing state.
- Structured JSON scan of iTerm DynamicProfiles — confirmed no `codex` Initial Text remains.

## Problems / Solutions

| Problem | Solution |
|---|---|
| User invoked `$handover` again immediately after a handover commit. | Created a short follow-up snapshot report rather than duplicating the full narrative. |
| Need to ensure the iTerm and Lead/Fable changes did not drift after commit/push. | Re-ran routing and DynamicProfile checks; all remained correct. |

## Outstanding Debts

| # | Priority | Debt | Next action |
|---|---|---|---|
| 1 | HIGH dated | Fable inclusion window ends 2026-07-07. `coding` and `hard` have scheduled reverts to `codex-cli:gpt-5.5`; `lead` has no scheduled revert. | On 2026-07-07 run `python3 ~/.claude/scripts/model_balancer.py decide --force`, check `lead/coding/hard`, and decide whether paid/usage-credit Fable should remain the Lead default. |
| 2 | MED | Codex retry/re-spawn telemetry is still incomplete, so `coding`/`hard` remain pinned. | Extend telemetry before unpinning active model scoring. |
| 3 | LOW | `weekly_tokens_cap` remains unset. | Dmitry can provide the weekly Max token allowance or leave the metric advisory. |
| 4 | LOW | `templates/commands/go.md` Phase 1 still needs provider-aware Flow Designer spawn mechanics. | Small spec edit in a future Booster session; verify with `tests/test_rule_updates.sh`. |

## Required Reading

- `reports/handover_2026-07-02_013025.md` — this final snapshot handover.
- `reports/handover_2026-07-02_012207.md` — main narrative for the Fable routing and iTerm DynamicProfile changes.
- `~/.claude/model_balancer.json` — live routing state; Lead/coding/hard currently route to Fable.
- `~/Library/Application Support/iTerm2/DynamicProfiles/{horizon,projects,social-downloader-bot,zonasvp}.json` — local terminal profile defaults currently launch `claude --model fable`.

## Session Reference

Session UUID: `019f1fc1-88de-7303-8e51-3eaacfb92d7f`
JSONL: `/Users/dmitrijnazarov/.codex/sessions/2026/07/02/rollout-2026-07-02T01-16-45-019f1fc1-88de-7303-8e51-3eaacfb92d7f.jsonl`

Grep this JSONL during RECON to see both handover runs, the Fable routing update, the iTerm DynamicProfile edit, verification checks, commits, and push.

## Recent Git Log

```text
b88826f docs: add handover for Fable routing update
```

## Verification Evidence

```text
git status --short --branch -> ## main...origin/main
python3 ~/.claude/scripts/model_balancer.py get lead -> {"provider":"anthropic","model":"fable"}
python3 ~/.claude/scripts/model_balancer.py get coding -> {"provider":"anthropic","model":"fable"}
python3 ~/.claude/scripts/model_balancer.py get hard -> {"provider":"anthropic","model":"fable"}
python3 ~/.claude/scripts/model_balancer.py get high_blast_radius -> {"provider":"anthropic","model":"claude-sonnet-4-6", ...}
DynamicProfiles JSON scan -> remaining_codex_initial_text=[], claude_model_fable_initial_text_count=12
```

## First Step Tomorrow

```bash
cd /Users/dmitrijnazarov/Projects/Claude_Booster
git status --short --branch
python3 ~/.claude/scripts/model_balancer.py get lead
python3 ~/.claude/scripts/model_balancer.py get coding
python3 ~/.claude/scripts/model_balancer.py get hard
```

Expected before 2026-07-07: all three return `anthropic:fable`.
