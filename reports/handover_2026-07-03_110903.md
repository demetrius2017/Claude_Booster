# Handover 2026-07-03 11:09:03 - Fable usage visibility and statusline guard

## Goal + KPI

- **North Star:** Claude Booster should expose premium-model usage clearly enough to prevent accidental budget burn without turning estimates into fake billing data.
- **Current milestone:** Add Fable 5 usage visibility, wire it into Fable/code-review flows, and fix status surfaces so cached cross-session estimates are not presented as current-session spend.
- **KPI:** Fable usage ledger is immutable and deduped; `$fable` / `code-review fable` refresh the visible estimate; statusline suppresses cross-session cached Fable values; bridge/statusline/Fable regression tests pass.

## Summary

This session began with `$start`, then pivoted because Dmitry caught a semantic defect in the new Fable usage display: the statusline showed `Fable $1.3642 MTD $889.9670`, which looked like current-session real spend. RECON confirmed that was misleading:

- `$1.3642` came from Claude session `a478b1c8-f1dd-4f4f-bc0e-4b6aa08eef46` in project `CRM-AI`, not this Codex `/start` session.
- `MTD $889.9670` was the July UTC aggregate from the Fable usage ledger: `2172` events across `12` sessions and `48` source paths.
- The code already labelled the ledger as "API-equivalent / credit-rate estimate; not an actual billing ledger", but the permanent status surfaces compressed that nuance away.

Repository state now contains the broader Fable usage visibility work:

1. Added `templates/scripts/fable_usage.py`:
   - parses Claude/Codex transcript JSONL for `claude-fable-5` assistant usage;
   - stores immutable deduped rows in `fable_usage_events`;
   - computes API-equivalent credit-rate estimates, not a billing ledger;
   - supports hook mode, `ingest`, `scan-month`, `summary`, `display`, and `refresh-display`.
2. Bumped rolling-memory schema to v9 in `templates/scripts/rolling_memory.py` and added the `fable_usage_events` table, indexes, and no-update/no-delete triggers.
3. Added a Stop hook entry in `templates/settings.json.template` for `fable_usage.py`.
4. Updated Fable and `code-review fable` command/skill/prompt docs so Fable usage is refreshed after an explicit Fable review/consult.
5. Added `tests/test_fable_usage.sh` for ledger parsing, dedupe, cost math, cache refresh, malformed input, statusline behavior, and hook/settings integration.
6. Adjusted `tests/test_claude_max_tracker.sh` so schema v9 does not break the older v8+ weekly usage assertion.
7. Hardened `tests/test_statusline.sh` to use a fake HOME so user-level Fable cache does not contaminate phase-format tests.
8. Fixed the display defect Dmitry reported:
   - `templates/scripts/statusline.sh` now prints `Fable session est $...` only when `last_task.session_id` in the cache matches the current statusline `session_id`;
   - it no longer prints MTD in the statusline;
   - `templates/scripts/fable_tmux_status.sh` prints `Fable last-task est $...` and no longer prints MTD.
9. Synced live `~/.claude/scripts/statusline.sh` from the template so installed statusline behavior matches the repo.
10. Stored rolling-memory feedback `46089`: Fable usage display must not imply real out-of-pocket spend or current-session spend when it is only an API-equivalent monthly ledger estimate.

No `roadmap.html` or `roadmap.md` exists in this repo. Roadmap update was skipped rather than creating a new roadmap file solely for the handover ritual.

## Tools used

- `sed`, `rg`, `nl`, `find`, `ls`, `date`
- `git`, `sqlite3`
- rolling memory MCP `memory_stats`, `memory_start_context`, `memory_search`, `memory_memorize`
- `bash tests/test_fable_usage.sh`
- `bash tests/test_statusline.sh`
- `bash tests/test_code_review_model_selector.sh`
- `python3 tests/test_install_codex_bridge_integration.py`
- `python3 -m py_compile templates/scripts/fable_usage.py`
- `git diff --check`
- `apply_patch`

## Verification

Latest verification before this handover:

```text
bash tests/test_fable_usage.sh
exit=0
Passed: 40
Failed: 0
RESULT: PASS

bash tests/test_statusline.sh
exit=0
Passed: 16
Failed: 0
RESULT: PASS

bash tests/test_code_review_model_selector.sh
exit=0
PASS code-review model selector contract

python3 tests/test_install_codex_bridge_integration.py
exit=0
Results: 10 passed, 0 failed

python3 -m py_compile templates/scripts/fable_usage.py
exit=0

git diff --check
exit=0
```

Manual display proof:

```text
printf '{"session_id":"not-a478b1c8"}' | ~/.claude/scripts/statusline.sh
exit=0
[RECON] 2/4 knowledge_base

printf '{"session_id":"a478b1c8-f1dd-4f4f-bc0e-4b6aa08eef46"}' | ~/.claude/scripts/statusline.sh
exit=0
[RECON] 2/4 knowledge_base | Fable session est $1.3642
```

The second command intentionally uses the CRM-AI Claude session id that owns the cached last Fable task. It proves the guard is session-scoped; unrelated sessions stay quiet.

## Problems / Solutions

| Problem | Solution |
|---|---|
| Fable statusline showed a cached monthly/task estimate as if it belonged to the current session. | Statusline now compares current `session_id` with cached `last_task.session_id`; mismatch suppresses the Fable label. |
| `MTD` in permanent status/tmux widgets looked like real money spent this session. | Removed MTD from statusline and tmux widget; command output still says month-to-date estimate and "not a billing ledger" when explicitly refreshed. |
| User-level `~/.claude/fable_usage_summary.json` could contaminate statusline tests. | `tests/test_statusline.sh` now runs statusline under a fake HOME. |
| Schema-specific claude_max test expected exactly v8, but Fable usage raises schema to v9. | `tests/test_claude_max_tracker.sh` now asserts schema `>= 8` for the `claude_max_usage` contract. |
| Future sessions could forget Dmitry's distinction between estimates, weekly-limit usage, and real money. | Saved rolling-memory feedback `46089` with the exact interpretation rule. |

## Outstanding Debts

No outstanding debts - session clean.

## Required reading

- `reports/handover_2026-07-03_110903.md` - this handover; start here for the Fable usage visibility and statusline guard.
- `templates/scripts/fable_usage.py` - source of truth for transcript parsing, immutable ledger writes, estimate math, and display cache semantics.
- `templates/scripts/statusline.sh` - session-id guard that prevents cross-session Fable cache leakage into the statusline.
- `templates/scripts/fable_tmux_status.sh` - tmux widget wording; deliberately omits MTD.
- `templates/scripts/rolling_memory.py` - schema v9 migration adds `fable_usage_events`.
- `templates/settings.json.template` - Stop hook wiring for `fable_usage.py`.
- `tests/test_fable_usage.sh` - regression test for ledger math, dedupe, cache refresh, and statusline suppression.
- `reports/handover_2026-07-02_200537.md` - prior `code-review fable` selector handover; explains why this usage display is tied to explicit Fable consult/review flows.
- `reports/handover_2026-07-02_180246.md` - model-routing burn-rate guard; explains why Fable remains permissioned and Codex gpt-5.5 is the default lane.

## Session reference

Session UUID: `019f26fa-9147-7f82-9f48-8d58d920ec93`  
JSONL: `/Users/dmitrijnazarov/.codex/sessions/2026/07/03/rollout-2026-07-03T10-56-23-019f26fa-9147-7f82-9f48-8d58d920ec93.jsonl`

This JSONL can be grepped during RECON to understand the exact sequence of `/start`, the Fable display diagnosis, verification commands, and this handover.

## Recent git log

```text
git log --oneline --since="8 hours ago"
exit=0
(no commits in the last 8 hours)
```

Current base before this handover commit:

```text
git rev-parse HEAD
exit=0
adde9925ecbf3be90ef6475b2330f443470ec21c

git ls-remote origin refs/heads/main
exit=0
adde9925ecbf3be90ef6475b2330f443470ec21c refs/heads/main
```

## First step tomorrow

Run:

```bash
cd /Users/dmitrijnazarov/Projects/Claude_Booster && \
sed -n '1,180p' templates/scripts/fable_usage.py && \
sed -n '88,112p' templates/scripts/statusline.sh && \
sed -n '1,30p' templates/scripts/fable_tmux_status.sh && \
bash tests/test_fable_usage.sh && \
bash tests/test_statusline.sh && \
python3 tests/test_install_codex_bridge_integration.py
```
