# Handover — Model-version refresh final verification

## Summary

Final verification completed for the already-implemented model-version refresh. The active routing defaults and installed copies now use Claude Opus 5 / Sonnet 5 / Haiku 4.5, Codex GPT-5.6 Sol/Terra/Luna, GLM-5.1, and Grok 4.6. A hostile scan found only historical reports, intentional migration entries, or negative fixtures after four stale active-facing template examples and one stale acceptance test were corrected. `python3 install.py --yes` synchronized `~/.claude` and the Codex bridge; no repository files were staged, committed, pushed, deleted, or changed under the protected untracked wait-patch paths.

## Goal + KPI

- **North Star:** model selection stays explicit, current, and reproducible across repository templates, installed Claude files, and the Codex bridge.
- **Current milestone:** complete and independently verify the model-version refresh without laundering historical migration evidence.
- **KPI:** all active default routes resolve only to the approved pins; `git diff --check`, audit smoke, full balancer smoke, and focused Python routing tests pass after install.

## Tools used

- `rg` hostile scans and `git diff`/`git diff --check` to distinguish active use from history, migration maps, and negative fixtures.
- `apply_patch` for four active-facing stale examples and the active-learning acceptance expectation.
- `python3 install.py --yes` to synchronize 18 changed installed files and the Codex bridge.
- `bash tests/test_audit_zai_smoke.sh`, `bash tests/test_model_balancer_all.sh`, focused `pytest`, and live `model_balancer.py` route probes.
- Cached advisory slice checks: `slice_ledger.py status` and `slice_telemetry.py status`.

## Verification evidence

- `git diff --check` → exit=0, both before and after the installed-copy synchronization.
- `python3 install.py --yes` → installed 138 files (18 written, 120 unchanged); bridge installed 22 skills, 20 prompts, and 20 command specs.
- `bash tests/test_audit_zai_smoke.sh` after install → PASS=14 FAIL=0, including installed/template equality.
- `bash tests/test_model_balancer_all.sh` after the test expectation refresh → 7/7 suites PASS, 127 aggregate assertions, exit=0.
- `pytest -q tests/test_gpt56_routing.py tests/test_grok_integration.py tests/test_zai_integration.py tests/test_codex_adapter_exact_route_contract.py tests/test_codex_capability_routing.py tests/test_consilium_external_fallback.py` → 77 passed in 4.01s, exit=0.
- `python3 ~/.claude/scripts/model_balancer.py decide --force` → active decision, 173 samples in 14 days, health_fallbacks=1. Live routes: `high_blast_radius=anthropic:claude-sonnet-5`; `audit_external=pal:gpt-5.6-sol`; `audit_secondary=zai-cli:glm-5.1`; `audit_tertiary=grok-cli:grok-4.6`; `hackathon_coder=grok-cli:grok-4.6`; `hackathon_external=grok-cli:grok-4.6` because active health logic currently falls back from GLM.
- `python3 ~/.claude/scripts/model_balancer.py status` → freshness=fresh, source=active, Codex Pro quota=100%. It still reports historical degraded telemetry for `grok-cli:grok-4.5` and `zai-cli:glm-5.2`; these are retained observations, not selected active pins.

## Problems / Solutions

- The first pre-install audit smoke had exactly one failure: C10 detected that the installed audit command had not yet been synchronized. `install.py --yes` resolved it; rerun passed 14/14.
- The full balancer smoke initially failed because its active-learning test asserted that old routes must remain unchanged and expected `claude-sonnet-4-6`. The test now asserts deterministic legacy-route migration to the approved active pins; rerun passed 27/27 in that suite.
- Historical reports, roadmap entries, migration maps, and negative fixtures retain retired names intentionally. They were not rewritten because doing so would destroy migration behavior or provenance.

## Outstanding Debts

| # | Priority | Description | Status |
|---|---|---|---|
| 1 | MED | `tests/test_lead_epistemic_anchors.py` has pre-existing errors from a missing `tmp` fixture; fix or register it as known-failing. | open |
| 2 | MED | Protected untracked `docs/codex_wait_patch.md`, `patches/`, and `scripts/codex_wait_patch/` remain pinned to an older Codex patch and need a separate rebase-vs-drop decision. | open; deliberately untouched |
| 3 | LOW | Shadow-mode timing calibration for DEVELOPMENT test mode remains outstanding. | follow-up |
| 4 | LOW | Grok `status` exit 69 (auth missing) and review exit 69 (empty response) still share a code; consider separating them. | open |

## Required reading

- `reports/handover_2026-09-03_202143.md` — this verification record, including the historical-telemetry caveat.
- `templates/scripts/model_balancer.py` — source of default pins, legacy migration, and health fallback behavior.
- `tests/test_model_balancer_active.sh` — acceptance contract that distinguishes a legacy migration from an unintended route change.
- `templates/scripts/supervisor/supervisor.py` — refreshed model-override help text; do not reintroduce retired pins in active CLI guidance.
- `roadmap.md` — current wait-history-patch scope; it was intentionally not updated by this unrelated model-routing verification.

## Slice telemetry

- Capability: Claude hooks/wrappers advisory; native Codex observational/no enforcement.
- Cached ledger: run `ef508f15-e8c3-4318-8c5f-043d4e3140c2`, session `019f7674-3f34-7a40-b9c9-b52ad26a2020`, state `closed`, terminal disposition `blocked`; it is a July historical slice unrelated to this session.
- Exact cached status command: `python3 ~/.claude/scripts/slice_telemetry.py --cwd /Users/dmitrijnazarov/Projects/Claude_Booster status --run-id ef508f15-e8c3-4318-8c5f-043d4e3140c2 --session-id 019f7674-3f34-7a40-b9c9-b52ad26a2020` → `status=not_recorded`, receipt path `.claude/state/runs/f67cac37ef55ea242313c3e3751f1f44821b3a50e89129c82240a6838a94c63b/slice_telemetry.json`, receipt hash absent, provider/adapter absent, parser coverage `unknown`.
- No current-session receipt/run binding exists in the cached ledger, so no telemetry was fabricated and no `record` command was run against an unrelated closed slice.

## Session reference

Session UUID: `01a06878-8ae7-7e61-9e10-87e531634b55`  JSONL: `/Users/dmitrijnazarov/.codex/sessions/2026/09/03/rollout-2026-09-03T20-12-05-01a06878-8ae7-7e61-9e10-87e531634b55.jsonl`

This Codex JSONL can be grepped during RECON to see the hostile scan, installed-copy sync, failing pre-sync C10, test-contract correction, and final live-route probes.

## First step

Run the current installed route and health snapshot before any further routing work:

```bash
python3 ~/.claude/scripts/model_balancer.py decide --force && python3 ~/.claude/scripts/model_balancer.py get high_blast_radius && python3 ~/.claude/scripts/model_balancer.py get audit_external && python3 ~/.claude/scripts/model_balancer.py get audit_secondary && python3 ~/.claude/scripts/model_balancer.py get audit_tertiary && python3 ~/.claude/scripts/model_balancer.py get hackathon_external && python3 ~/.claude/scripts/model_balancer.py get hackathon_coder && python3 ~/.claude/scripts/model_balancer.py status
```
