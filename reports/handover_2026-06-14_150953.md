# Handover — 2026-06-14 15:09 MSK — Codex delegate effort pinned to medium

## Summary

This session fixed the specific Codex delegation problem Dmitry reported: new delegated agents could still appear as `Spawned ... (gpt-5.5 high)` even though `~/.codex/config.toml` has `model_reasoning_effort = "medium"`.

Root cause: there are two delegation paths, and both needed explicit handling.

1. Shell-based Booster Codex delegates (`codex_worker.sh`, `codex_sandbox_worker.sh`) called `codex exec -m "$MODEL"` without an explicit effort override. They now pass `-c model_reasoning_effort="medium"` by default, with `CODEX_REASONING_EFFORT=<low|medium|high|xhigh>` as an intentional override.
2. Native Codex `spawn_agent` inherits the Lead session's effort when `reasoning_effort` is omitted. The Booster Codex bridge skill and tool-strategy rule now explicitly require `reasoning_effort: "medium"` for delegated Codex subagents unless the user asks for higher effort.

Installed runtime copies were updated immediately:

- `~/.claude/scripts/codex_worker.sh`
- `~/.claude/scripts/codex_sandbox_worker.sh`
- `~/.agents/skills/booster-command/SKILL.md`
- `~/.claude/rules/tool-strategy.md`

Repo templates were updated for future installs. No roadmap file exists in this repository (`roadmap.html` and `roadmap.md` absent), so no roadmap update was possible.

## Goal + KPI

- **North Star:** Claude Booster makes Claude Code and Codex remember, learn, audit, and route work across sessions without re-teaching or accidental budget burn.
- **Current milestone:** Keep Codex delegation cheap and predictable while preserving the Шестёрка cross-provider quality pipeline.
- **KPI:** delegated Codex workers and Codex subagents should run at `medium` effort by default; `/go` KPI remains healthy at `task_count=3`, `first_pass_clean_rate=100.00%`, `mean_verifier_fail_count=0.00`, and all defect category totals at `0`.

## Tools used

- `$start` at session start: README, latest handover, architecture map, memory, canary, telemetry, KPI.
- Shell RECON with `rg`, `sed`, `git diff`, `codex exec --help`, and fake `CODEX_BIN` probes.
- `apply_patch` edits to repo templates and installed runtime copies.
- Verification commands:
  - `python3 templates/scripts/test_model_metric_capture.py`
  - `bash tests/test_codex_regex_fix.sh`
  - `bash -n` on template + installed wrapper scripts
  - fake `CODEX_BIN` smoke tests for both wrappers
  - `git diff --check`

## Problems / Solutions

| Problem | Solution |
|---|---|
| `spawn_agent` showed `gpt-5.5 high` for delegated agents. | Codex subagents inherit Lead effort unless `reasoning_effort` is passed. Updated Booster Codex bridge and tool-strategy to require `reasoning_effort: "medium"` for delegates by default. |
| Shell wrappers did not force Codex effort. | Added `-c model_reasoning_effort="$CODEX_REASONING_EFFORT"` with default `medium` to both `codex_worker.sh` and `codex_sandbox_worker.sh`. |
| `codex_worker.sh` could not be fake-tested cleanly because `CODEX_BIN` was hardcoded. | Changed it to `CODEX_BIN="${CODEX_BIN:-/opt/homebrew/bin/codex}"`, matching the sandbox wrapper pattern. |
| A new already-running parallel agent `019ec607-7a1a-7381-ab82-48ea822fce17` still showed `gpt-5.5 high`. | Expected: already spawned agents do not hot-reload changed rules/skills. New spawns must explicitly pass `reasoning_effort: "medium"`; old agents need restart if their effort matters. |
| `tests/test_codex_sandbox_worker.sh` still reports `29/30` instead of `30/30`. | The remaining failure is pre-existing byte-drift between `templates/rules/tool-strategy.md` and `~/.claude/rules/tool-strategy.md`. The new effort rule exists in both. Do not blindly sync the entire file: the installed copy contains local newer routing policy (`coding` via Opus) not present in the template. |

## Verification

- `python3 templates/scripts/test_model_metric_capture.py` → `Results: 11 passed, 0 failed`, `exit=0`.
- `bash tests/test_codex_regex_fix.sh` → `Results: 23/23 passed`, `exit=0`.
- `bash -n templates/scripts/codex_worker.sh templates/scripts/codex_sandbox_worker.sh ~/.claude/scripts/codex_worker.sh ~/.claude/scripts/codex_sandbox_worker.sh` → `exit=0`.
- Fake `CODEX_BIN` for `templates/scripts/codex_worker.sh gpt-5.5` captured: `exec -c model_reasoning_effort="medium" --skip-git-repo-check -m gpt-5.5 -`, `exit=0`.
- Fake `CODEX_BIN` for `templates/scripts/codex_sandbox_worker.sh gpt-5.5` captured: `exec -c model_reasoning_effort="medium" -C <worktree> -s workspace-write --ephemeral -m gpt-5.5 -`, `exit=0`.
- `git diff --check` → `exit=0`.

## Outstanding Debts

| # | Priority | Description | Status |
|---|---:|---|---|
| 1 | LOW | RECON-as-review harvest is circular on "fix X" tasks: it can surface the Objective itself as an `adjacent_finding`. Teach the harvest to skip findings that merely restate the Objective. | open |
| 2 | LOW | `go.md` cosmetic advisory from debt #2 diff-review: Phase 1 parameterizes `<model>` while 1B/3B pin `gpt-5.5`; wording around "piping"/`<` is slightly imprecise. | open |
| 3 | MED | Unpin `coding`/`hard` from `_PINNED_CATEGORIES` only after ~14 days of real Codex coding/hard metrics accumulate. Capture is fixed; this is time-gated, not agent-actionable now. | deferred |
| B1 | BLOCKED | `weekly_tokens_cap` requires Dmitry's Claude Max weekly token allowance. Until set, balancer uses the stale 86% snapshot; harmless. | needs human |
| B2 | LOW | Decide whether to intentionally reconcile template vs installed `tool-strategy.md`. Current drift is known and should not be flattened blindly. | open |

## Required reading

- `reports/handover_2026-06-14_150953.md` — this handover; contains the exact effort fix and verification evidence.
- `templates/scripts/codex_worker.sh` — read before changing text-only Codex delegation; default effort now lives here.
- `templates/scripts/codex_sandbox_worker.sh` — read before changing code-writing Codex delegation; default effort now lives here too.
- `templates/codex/skills/booster-command/SKILL.md` — Codex bridge rule requiring `reasoning_effort: "medium"` on native subagents.
- `templates/rules/tool-strategy.md` and `~/.claude/rules/tool-strategy.md` — both contain the new effort rule, but the installed copy has known local policy drift; compare before syncing.
- `tests/test_codex_sandbox_worker.sh` — contains the current 29/30 caveat caused by known `tool-strategy.md` drift.

## Session reference

Session UUID: `019ec602-eb09-7492-9fb0-21b38ac33dba`  
JSONL: `/Users/dmitrijnazarov/.codex/sessions/2026/06/14/rollout-2026-06-14T15-02-20-019ec602-eb09-7492-9fb0-21b38ac33dba.jsonl`

Parallel spawned agent observed by Dmitry: `019ec607-7a1a-7381-ab82-48ea822fce17` at `/Users/dmitrijnazarov/.codex/sessions/2026/06/14/rollout-2026-06-14T15-07-19-019ec607-7a1a-7381-ab82-48ea822fce17.jsonl`.

Grep these JSONLs during RECON to see the report of `gpt-5.5 high`, the wrapper fix, the fake `CODEX_BIN` verification, and the hot-reload explanation for already-spawned agents.

## First step tomorrow

Confirm the delegate-effort contract and then decide whether to clean up the known rule drift:

```bash
cd ~/Projects/Claude_Booster
bash -n templates/scripts/codex_worker.sh templates/scripts/codex_sandbox_worker.sh ~/.claude/scripts/codex_worker.sh ~/.claude/scripts/codex_sandbox_worker.sh
python3 templates/scripts/test_model_metric_capture.py | tail -1
bash tests/test_codex_regex_fix.sh | tail -4
rg -n 'reasoning_effort: "medium"|model_reasoning_effort|CODEX_REASONING_EFFORT' templates/codex/skills/booster-command/SKILL.md ~/.agents/skills/booster-command/SKILL.md templates/rules/tool-strategy.md ~/.claude/rules/tool-strategy.md templates/scripts/codex_worker.sh ~/.claude/scripts/codex_worker.sh templates/scripts/codex_sandbox_worker.sh ~/.claude/scripts/codex_sandbox_worker.sh
```

Then either resolve debt #1 (skip Objective-restating RECON-as-review findings) or explicitly decide how to reconcile `tool-strategy.md` template vs installed drift.
