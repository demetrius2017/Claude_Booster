<!-- BEGIN BOOSTER SESSION ROLE -->
Default execution mode for a new conversation is `developer`: implement and
integrate directly; delegate useful bounded research and independent testing.
You may run immediate checks yourself. Do not create helpers without useful work.
The main agent remains responsible for the complete user-visible result.
To select `developer` or `lead`, or inspect `status`, recognize an explicit leading
`/role`, `role`, or `$role` and read `~/.agents/skills/role/SKILL.md` directly.
Retain the selected mode and reusable role-agent IDs within this conversation;
never infer them from another conversation. Do not load the general Booster
runner for this switch. Explicit command contracts still apply when requested.
<!-- END BOOSTER SESSION ROLE -->
